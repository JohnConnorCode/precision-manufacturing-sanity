'use client'

// Placeholder for Sanity Visual Editing. This returns null until the
// @sanity/visual-editing package is installed and wired.
// To enable later, replace this with:
//   import { VisualEditing } from '@sanity/visual-editing/next'
//   export default function SanityVisualEditing() { return <VisualEditing /> }

export default function SanityVisualEditing() {
  return null
}

